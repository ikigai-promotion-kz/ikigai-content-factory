/**
 * Демо-конфиг: проверка, что фабрика собралась правильно.
 *
 * Запуск из папки engine:
 *   node reels/montage/run.mjs demo/demo.config.mjs
 *
 * Все пути внутри считаются от этого файла, так что из какой папки запускать — не важно.
 * Ноль кредитов: ffmpeg и браузер работают на вашей машине.
 */

export default {
  name: 'demo-reel',

  // Что монтируем и откуда берём слова с таймкодами.
  src: './demo.mp4',
  wordsPath: './words.json',
  outDir: './out',

  brand: 'IKIGAI PROMOTION',
  accent: '#E5231B',

  // Слова-акценты: подсвечиваются в субтитрах и тянут кадр на крупный план.
  keywords: ['контент', 'фабрик'],

  // Хук держит первые секунды и открывает петлю любопытства.
  hook: { text: 'Этот ролик смонтировал код', t1: 1.6 },

  // Биты привязаны к НОМЕРАМ ФРАЗ — их список печатается при каждом прогоне.
  beats: [
    { phrase: 2, card: { title: 'Без монтажёра', sub: 'нарезка, титры, субтитры — скриптом' } },
  ],

  cta: { title: 'Смонтировано кодом', sub: 'ноль кредитов, всё на вашей машине', hold: 1.8 },
};
